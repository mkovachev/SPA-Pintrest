<div class="jumbotron"> ​
    <picture>
        <img class="rounded" src={{image.large}}>
    </picture>
    <h1>{{title}}</h1>
    <h6>Date: {{date}}</h6>
    <h6>City: {{city}}</h6>
    <h6>Category: {{category}}</h6>
    <p>Description: {{description}}</p>
</div>